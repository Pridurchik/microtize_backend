ktor:
  deployment:
    port: 8080
    host: 0.0.0.0
  application:
    modules:
      - com.garun.ApplicationKt.module

# Значения ниже используются, если не заданы переменные окружения
# ADMIN_USERNAME / ADMIN_PASSWORD / CORS_HOST / DB_PATH (см. docker-compose.yml)
admin:
  username: admin
  password: secret123

cors:
  host: microtize.ru
